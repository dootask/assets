# ===========================================
# DooTask AI 智能体插件 - 环境配置
# 复制此文件为 .env 并填入实际配置值
# ===========================================

# 🚀 应用基础配置
NODE_ENV=development
APP_NAME="DooTask AI Agent"
APP_VERSION=1.0.0

# 🔌 服务端口配置
APP_PORT=3000                    # Next.js 前端端口
GO_SERVICE_PORT=8080             # Go 主服务端口  
PYTHON_AI_SERVICE_PORT=8001      # Python AI 服务端口

# 🗄️ 数据库配置
DATABASE_URL=postgresql://dootask:dootask123@localhost:5432/dootask_ai?sslmode=disable
REDIS_URL=redis://localhost:6379/0

# 🔐 认证配置
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=24h

# 🔗 DooTask 集成配置
DOOTASK_API_BASE_URL=http://localhost:7001
DOOTASK_API_TOKEN=your-dootask-api-token
DOOTASK_WEBHOOK_SECRET=your-webhook-secret-token

# 🌐 前端配置
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api
CORS_ORIGIN=http://localhost:3000,http://localhost:3001

# 📁 文件上传配置
UPLOAD_MAX_SIZE=10485760         # 10MB
UPLOAD_ALLOWED_TYPES=pdf,doc,docx,txt,md,xlsx,xls

# 💡 注意：以下配置已移至 Web 管理界面
# - AI 模型配置 (OpenAI、Claude、Gemini 等)
# - MCP 工具配置 (Google 搜索、天气查询等)
# - 智能体参数配置
# 请在系统设置页面进行配置管理

# 📊 开发环境配置
LOG_LEVEL=info
ENABLE_DEBUG=false
ENABLE_SWAGGER=true

# 🔒 生产环境注意事项
# 在生产环境中请确保：
# 1. 修改 JWT_SECRET 为更强的密钥 (至少 32 字符)
# 2. 修改数据库密码为更安全的密码
# 3. 使用真实的 API 密钥替换所有占位符
# 4. 将 ENABLE_DEBUG 设为 false
# 5. 将 ENABLE_SWAGGER 设为 false
# 6. 配置正确的 CORS_ORIGIN 域名 