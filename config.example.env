# ===========================================
# 企业固定资产管理系统 - 环境配置
# 复制此文件为 .env 并填入实际配置值
# ===========================================

# 🚀 应用基础配置
NODE_ENV=development
APP_NAME="企业固定资产管理系统"
APP_VERSION=1.0.0

# 🔌 服务端口配置
APP_PORT=3000
GO_SERVICE_PORT=8000

# 🗄️ 数据库配置 (SQLite)
DATABASE_TYPE=sqlite
SQLITE_DB_PATH=./data/assets.db

# 📁 文件上传配置
UPLOAD_DIR=./public/uploads
UPLOAD_MAX_SIZE=10485760         # 10MB
UPLOAD_ALLOWED_TYPES=jpg,jpeg,png,gif,pdf,doc,docx,xlsx,xls

# 📊 开发环境配置
LOG_LEVEL=info
ENABLE_DEBUG=true
ENABLE_SWAGGER=false

# 🌐 前端配置
NEXT_OUTPUT_MODE=                # 输出模式
NEXT_PUBLIC_BASE_PATH=           # 基础路径
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# 🔒 安全配置
JWT_SECRET=dev-jwt-secret-key
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# 📈 性能配置
MAX_CONCURRENT_REQUESTS=100
REQUEST_TIMEOUT=30
STATIC_FILE_CACHE_DURATION=3600  # 1小时

# 🔒 生产环境注意事项
# 在生产环境中请确保：
# 1. 将 NODE_ENV 设为 production
# 2. 将 ENABLE_DEBUG 设为 false
# 3. 更改 JWT_SECRET 为安全的密钥
# 4. 配置正确的 CORS_ORIGINS
# 5. 根据需要调整 UPLOAD_MAX_SIZE 和 UPLOAD_ALLOWED_TYPES
